tags:
  - Messages
summary: Query message subscriptions
description: >
  Performs a message subscription query.
parameters:
  - $ref: "#/components/parameters/offset"
  - $ref: "#/components/parameters/limit"
requestBody:
  content:
    application/json:
      schema:
        $ref: "#/components/schemas/MessageSubscriptionCriteria"
      examples:
        by_partition_and_process_instance_id:
          summary: By partition and process instance ID
          value:
            partition: "2025-04-11"
            processInstanceId: 576
responses:
  "200":
    description: OK
    content:
      application/json:
        schema:
          $ref: "#/components/schemas/MessageSubscriptionRes"
        example:
          count: 1
          results:
            - id: 7
              partition: "2025-05-01"
              elementId: 10
              elementInstanceId: 4259
              processId: 2
              processInstanceId: 576
              bpmnElementId: messageCatchEvent
              correlationKey: example-key
              createdAt: "2025-05-01T19:25:42.749Z"
              createdBy: go-bpmn
              name: example-message
  "400":
    description: Bad request
    content:
      application/problem+json:
        schema:
          $ref: "#/components/schemas/Problem"
        examples:
          invalid_limit:
            $ref: "#/components/examples/QueryLimitProblem"
          invalid_offset:
            $ref: "#/components/examples/QueryOffsetProblem"
